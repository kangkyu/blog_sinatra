### Run local server

```sh
gem install bundler # if not done yet
bundle install
rerun 'rackup'
```

### Readme-Driven Steps

+ <del>save data as text files.</del>
+ <del>index and create</del>
+ edit
+ delete
+ style
